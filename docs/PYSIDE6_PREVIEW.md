# PySide6 界面迁移预览

`pyside_preview.py` 是独立的视觉和交互原型，不替换 `run_auto_rng_gui.py`，也不会修改配置、搜索方案或启动 EasyCon。

2026-09-06 新增独立的 `run_pyside6_gui.py` / `启动-PySide6功能版.bat`，优先接入野生 / 静态与共用服务。实际接入范围、启动方法及验收边界见 [功能版说明](PYSIDE6_FUNCTIONAL.md)。下文的“未接入”描述针对纯预览入口。

## 设计原则

工具图标采用用户提供的 `Sleep_0255-3_Sprite.webp` 火稚鸡图片：原始文件保留为 `assets/pyside_preview/torchic-source.webp`，按非透明图案范围适配为 PNG 与多尺寸 ICO。图案在画布内向下微调约 3%（小尺寸至少 1 px），使角色视觉居中，保持完整图案。PySide6 的窗口、任务栏及侧栏标识使用同一图案；Windows 使用独立应用标识。正式 Tk 入口与发布打包脚本尚未改动。

工具的目标是让人容易操作。以用户确认的 `7438e0e` 初版截图作为视觉基准：紧凑的存档/设备摘要、左侧常用条件、右侧方案与运行准备、单行底部主操作。功能映射用于核对完整性，不要求照搬 Tk 的控件排列或把全部参数同时铺开。常用输入直接显示，少用设置可展开，重要确认与警告保持可见。

## 当前范围（2026-09-05 页面核对）

- 以提交 `7438e0e` 的 `pyside_preview.py` 为初版，先完成 [逐页功能映射](PYSIDE6_PAGE_MAP.md)，再调整布局。该表列出了正式 Tk 字段、处理函数、原型缺项与接入边界。
- 侧栏顺序与 Tk 一致：SID 查找、TID 乱数、TID 实测表、野生 / 静态、孵蛋；高级模式显示脚本测试，运行日志常驻末尾。默认打开 SID。
- 恢复初版深色侧栏、白色卡片与约 7:4 的双栏比例，默认 1360×860，最小 900×620。右上角为两张紧凑摘要卡：点击存档打开选择/手动身份输入，点击设备打开共通设置；右侧集中呈现方案和三项运行准备，结果详情另开窗口。
- 底栏直接显示生成、取消搜索、开始运行、停止 EasyCon，按准备/取消与开始/停止成对排列，不使用更多菜单隐藏操作。窗口窄于 1180 时隐藏底栏说明文字，把空间留给全部按钮，并通过“方案与设备”查看同一份右侧内容；实测表使用整页宽度。各页输入纵向滚动，右侧状态保持可见。表格和日志保留内部滚动。
- IV 恢复六张能力小卡，点击能力名称可重置单项。SID 按队伍顺序逐只填写，六槽仍完整保留，数量外的槽位禁用且不清除输入；孵蛋亲本 A/B 各占紧凑一行，性别和六项 IV 并排，输入统一 36 px 高，保留增减按钮和悬浮说明，无须横向滚动。TID 细调、脚本路径、更多筛选和标签诊断可按需展开；折叠不删除输入。
- 存档窗口第一行是“游戏版本（火红/叶绿）、ROM 语言/地区（英文/美版、日文/日版）”，第二行是当前 TID/SID。游戏版本与野生/静态、孵蛋页双向同步，语言与 TID 页同步。遵循 Tk 应用存档的行为，从存档窗口修改语言不会覆盖已输入的目标身份或延迟；只修改本次窗口中的输入，存档持久化与运行后端仍未接入。
- SID 和 TID 独立脚本路径移至“共通设置”，分别保留“SID 查找脚本”和“TID 1.3.7 脚本包”分区；主页面不再放置路径选择卡片。
- 共通设置移除重复的设备检测、虚拟手柄与监视窗口按钮，这三个入口统一位于“运行准备”；窄屏通过“方案与设备”打开同一处。设置内保留路径选择与两种更新操作，按两列排列。
- “脚本日志设置”置于共通设置最上方，打开窗口即可选择日志等级；设备和独立脚本路径依次放在其后。
- 顶部“高级模式”左侧直接放置“低分自适应、命中后更新预校准、Seed 校准、Seed 启动”。前两项使用与高级模式一致的切换按钮，开启时显示浅蓝底与勾选标记，固定宽度避免切换时跳动，原悬浮说明保留。窄窗口分成两行，所有控件仍可见；Seed 选项用简短名称，悬停查看完整方案说明。默认值、模式范围和高级开关的启用规则沿用原预览；脚本输出日志留在共通设置。
- 顶部使用同行的“高级模式｜设置”组合控件：左侧切换模式，开启时以浅蓝底与勾选标记表示；右侧打开高级设置，关闭模式后变灰，控件位置不跳动。高级窗口集中放置脚本入口、奇偶与三层扩窗，不放跨页面跳转按钮。关闭窗口保留输入，关闭高级模式仍按正式约定恢复 Seed / 入口 / PID 默认值。
- 设置条去掉重复的“界面预览”字样，所有按钮 / Seed 下拉框统一 36 px 高度；Seed 标题改放在下拉框左侧，整排居中对齐。窄窗口按钮与 Seed 分成两行；底栏未接入状态与禁用操作继续按真实状态显示。
- 对照 Tk 的悬浮说明补齐各页标题 `?` 与相关输入、复选框、操作按钮的提示，支持悬停和点击查看；完整映射见 [悬浮说明与高级模式核对](PYSIDE6_HELP_MAP.md)。补充 TID 额外目标、接近区间自动转乱数及其参数，仅为内存输入；同时修正旧预览错误的 TID 帧数单位说明。
- 2026-09-06 将核对后的正式脚本参数注释整理到相应字段：SID 逐只读档与 EV、TID 名称 / 延迟 / 去噪、孵蛋 Held / Pickup / 亲本、Seed 启动与奇偶。标签和输入都能悬停查看，小字段不加问号；旧注释与当前校验冲突时以现有正式行为为准，来源记录在帮助映射中。
- 下拉弹层统一使用白底、细描边、圆角、浅蓝选中态与宽松行间距；长列表限制显示高度并保留滚动和键盘选择。实际展开检查的截图在 `.build/pyside-simple/popup-*.png`。
- 下拉框右侧统一显式绘制 SVG 向下箭头，修复自定义 `drop-down` 样式后缺少展开符号的问题；复用现有 chevron-down 图标，普通输入、紧凑筛选、亲本性别及禁用状态均可见。正常 Windows 事件循环中逐项打开列表检查，截图为 `.build/pyside-simple/combo-arrow-*.png`。
- Windows 下透明弹层同时设置无边框标志，避免圆角外露出黑色背景。已通过屏幕区域截图复现修复前的黑角，并检查长 Seed 列表与短语言列表修复后的四角；证据在 `.build/pyside-simple/corner-*.png`，另验证末项选择、Enter 确认及 Escape 关闭。
- 数值框右侧明确显示上下增减按钮，使用统一底色和箭头；已在正常窗口中实际点击验证增减与上限，并检查 900×620 窗口中的个体值输入。截图在 `.build/pyside-simple/spin-*.png`。
- SID 补齐六槽、初始等级、野生地点、EV、糖果/阈值/机型/确认；TID 恢复正式七分区；实测表保留完整 11 列；野生/静态补齐身份、指定模式、觉醒力量和 SID 遍历；孵蛋补齐运行条件、亲本与四种配置操作；日志恢复设备标签诊断布局。
- 日志页将“设备标签诊断与覆盖”放在内容底部，收起时日志自动占满其上方剩余高度；展开诊断时日志缩小，窗口不足时保留页面滚动以访问全部诊断操作。
- 野生 / 静态的“觉醒力量”与闪光、性格、特性、性别放在个体值卡片的同一行，顺序依次为这五项；不再收在“更多筛选与出闪后处理”中。选项与输入状态保持原样，闪光 / 特性列稍宽以容纳长文字。
- 野生 / 静态仅“道具乱数”和“SID 遍历”使用两个独立折叠栏，默认收起，分别容纳模式开关与各自参数。“出闪后处理”的抓捕、麻痹、点到为止直接显示；原“更多筛选与出闪后处理”外层折叠移除。展开 / 收起不启停模式，不清空参数，原有野生限制和互斥规则保留。
- 出闪后处理的三个选项改成与高级模式一致的切换按钮，36 px 高、固定宽度，开启显示浅蓝底与勾选；保持三个独立状态及悬浮说明，仍只修改本窗口表单。
- 可以实际操作的内容仅为内存表单：导航、高级显隐、SID 槽位数量、IV 预设/单项重置、模式互斥、TID 条件禁用/语言默认值、野生与孵蛋的游戏/主机同步、可选参数折叠及存档/结果/共通设置窗口。
- 真实存档管理、设备检测、路径选择/校验、数据库、配置保存/载入、遭遇目录、搜索、SID 计算、预检、生成、日志尾读、标签覆盖和运行器均未接入，相应按钮禁用并有说明。没有虚构的已连接设备、已就绪计划或成功日志。Qt 不导入 Tk、计划器或乱数算法模块，只复用静态界面译名。
- 表单切换不代表业务验证通过；自动化游戏操作、冻结入口和正式 Tk 均不变。

## 启动

2026-09-06 对照正式 Tk 至 `b33b57f` 同步 TID 增量：局部搜索一轮后返回穷举、只裁剪负向越界范围、非零半径乱数续跑与最近 16 个完成区域。续跑区改名“参数保存与搜索续跑”并保持直接显示；每轮日志格式和机器检查点过滤边界放入日志浮窗，日志尾读 / 过滤仍未接入。15 项界面检查通过，正常 Windows 窗口实查 TID 与日志页的 1360×860 / 900×620 及三个浮窗；截图在 `.build/pyside-simple/tid-sync-*.png`。

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements-pyside-preview.txt
.\.venv\Scripts\python.exe pyside_preview.py
```

或双击 `启动-PySide6界面预览.bat`。

指定页面并输出截图：

```powershell
.\.venv\Scripts\python.exe pyside_preview.py --page wild --screenshot .build\pyside-preview.png
.\.venv\Scripts\python.exe pyside_preview.py --page tid --size 900x620 --scroll 1 --screenshot .build\pyside-tid-bottom.png
.\.venv\Scripts\python.exe pyside_preview.py --page tid --expand --scroll 1 --screenshot .build\pyside-tid-expanded.png
.\.venv\Scripts\python.exe pyside_preview.py --page egg --advanced --settings --scroll 1 --screenshot .build\pyside-settings.png
```

Windows 本地视觉验收应使用上面的正常图形后端，使 Qt 载入系统中文字体。自动化测试会单独使用 `offscreen` 后端，只验证窗口能生成合法 PNG，不把该截图当作字体视觉验收结果。

页面键为 `sid / tid / tid_records / wild / egg / script_test / logs`；`--scroll` 为 0–1 的纵向位置，`--settings` 改为打开并截图共通设置，`--advanced` 展示高级控件，`--expand` 展开可选分区便于视觉检查。`--page script_test` 会自动开启高级模式。截图命令实际 `show()` 窗口，在 Qt 布局稳定后保存 PNG 并退出；正常启动不带 `--screenshot`。

本轮已逐页启动 Windows 正常图形后端窗口并检查截图，额外核对全部七页的 900×620 底部及共通设置。图片保存在本机忽略目录 `.build/pyside-migration/`。Windows computer-use 截图接口复核后仍返回 `SetIsBorderRequired ... 0x80004002`，所以视觉证据来自已显示窗口的 Qt `grab()`，不是 offscreen 图像。检查修复了小窗口 IV 控件把整页撑宽的问题。

恢复初版设计后再次逐页启动 Windows 正常窗口检查，截图保存在 `.build/pyside-simple/`，包括 SID 分槽、A/B 亲本、整页实测表和 900×620 展开分区。

`tests/test_pyside_preview.py` 共 15 项定向检查，覆盖独立导入、PNG、页面上下文保留、六槽禁用、精确 IV 预设、野生互斥/共用身份、TID 接续/延迟与新参数保留、Seed 高级范围、高级窗口可达、帮助点击与禁用说明、后端按钮禁用、最小窗口与全部分区底部可达性、窄屏方案弹窗返回、折叠后输入保留。CI 安装可选 PySide6 依赖执行这些检查，正式程序依赖表不变。测试与视觉检查均不代表后端迁移或 Switch 实机验收完成。

## 后续接入顺序

1. 按页面映射逐项抽取已有输入模型、校验与服务接口，再替换预览里的默认值和目录占位。
2. 优先接入存档信息、端口/采集卡检测、TID 实测表和日志，再接入野生/静态搜索。
3. 接入 SID、TID、孵蛋与脚本运行状态机时，复用已验证业务代码；只有真实调用并验证后才启用对应按钮。
4. 保留正式 Tk 入口作为回退；功能版已开始接入野生 / 静态，剩余页面继续按上述服务边界迁移，不把可编辑表单计为功能已迁移。

# 原生 EasyCon 代码来源

`easycon/native/`、`easycon/native_backend.py` 与 `capture_broker*.py` 移植并改写自 `auto-bdsp-rng` 项目的原生 EasyCon 实现（GPL-3.0-or-later）。本项目保留其许可证义务；改动部分由本项目维护。

上游参考路径：`src/auto_bdsp_rng/automation/easycon/native/`、`native_backend.py`、`capture_broker.py` 和 `capture_broker_process.py`。

# 1.6.4-a 共享反查导航：队伍页按上移次数选择目标。
# 本段由 Python 固化到正式版与时间轴版主脚本，不单独执行。

FUNC 反查_队伍页按上移次数选择目标($上移次数: INT): INT
    IF $上移次数 < 1 or $上移次数 > 5
        PRINT 反查队伍槽上移次数无效: & $上移次数
        RETURN 0
    ENDIF
    FOR $反查队伍槽上移索引 = 1 TO $上移次数
        UP
        WAIT 500
    NEXT
    RETURN 1
ENDFUNC
